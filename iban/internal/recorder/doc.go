// Package recorder captures microphone audio as a detached process and
// manages its lifecycle plus a watchdog re-exec. PipeWire backs Unix
// sessions, DirectShow through ffmpeg backs Windows.
package recorder
